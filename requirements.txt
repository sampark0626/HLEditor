numpy>=2.4,<3
scipy>=1.17,<2
google-genai>=2.6,<3
flask>=3.1,<4
google-api-python-client>=2.197,<3
google-auth-oauthlib>=1.4,<2
google-auth-httplib2>=0.4,<0.5
requests>=2.34,<3
pillow>=12.2,<13
pystray>=0.19,<0.20

# ── dot-play(FM 스타일 2D 버드뷰) 파이프라인 ────────────────────────────────
# 이 블록은 무겁다(torch만 수백MB~2GB+). 첫 설치 시 시간이 오래 걸릴 수 있으나
# HLEditor 자체 시작 속도에는 영향 없음(dotplay 모듈은 잡 실행 시점에만 import됨).
opencv-python>=4.9
supervision>=0.24
pandas>=2.1
pyarrow>=15.0
tqdm>=4.66
torch>=2.2
torchvision>=0.17
ultralytics>=8.3
transformers>=4.44
timm>=1.0
umap-learn>=0.5
scikit-learn>=1.4
# roboflow의 'inference' 패키지는 Python 3.14 휠이 없어(상한 <3.12~3.13) 쓰지 않는다
# — dotplay/roboflow_client.py가 requests로 REST API를 직접 호출한다.
git+https://github.com/roboflow/sports.git
